spring:
  datasource:
    url: jdbc:mysql://localhost:3306/catchupdatabase
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org:
      hibernate: warn
---
server:
  port: 8081
